import { FC, useState } from "react";
import Card from "./Card";
// import { useFetchData } from "../api/queries";

const Books: FC = () => {
  // const { data = [], error, isLoading } = useFetchData();
  return (
    <div>
      <Card />
    </div>
  );
};

export default Books;
