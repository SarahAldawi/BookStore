import { FC } from "react";
import { DesignSection, Hero } from "../components";

const HomePage: FC = () => {
  return (
    <div>
      <Hero />
      <DesignSection />
    </div>
  );
};

export default HomePage;
